#include <iostream> //Input and output stream which declares object that control reading from and writing to the standard streams
using namespace std; //To use names for objects and variable from the standard library

int main () //defines the entry or the starting point of the C/C++ program code.
{
cout << "Hello Philippines"; //cout is use for displaying the output to the screen, while Hello Philippines is the object
}
